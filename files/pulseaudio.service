# {{ ansible_managed }}
[Unit]
Description=PulseAudio
After=bluetooth.target sound.target

[Service]
ExecStart=/usr/bin/pulseaudio --start --disallow-exit --disable-shm
User={{ a2dp_sink_user }}
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
